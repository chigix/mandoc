import * as path from 'path';

export const FIXTURES_DIR = __dirname;
export const TPL_TEST_FIXTURE = path.join(FIXTURES_DIR, './tpl-test');
export const RENDER_CASE_FIXTURE = path.join(FIXTURES_DIR, './tpl-render-result');
